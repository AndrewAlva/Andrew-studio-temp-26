// Passthrough vertex shader for the window gradient/blur plane
// TODO: implement

varying vec2 vUv;

void main() {
  vUv = uv;
  gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
}
